# API Тестирование

Проект автоматизированных тестов для внешнего API с использованием Python, `requests` и `pytest`.

## 📦 Требования

- Python 3.8+
- pip

## Установка

1. Клонируйте репозиторий:
   ```bash
   git clone <ваш-репозиторий>
   cd api-tests
  
## 🚀 Запуск

 -  python -m pytest -s -v